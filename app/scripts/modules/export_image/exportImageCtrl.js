(function(app, ng) {
    app.controller('ExportImageController', function($scope) {
        var vm = $scope.vm = {
            name: 'export_image'
        };
    });
})(angular.module('opApp'), angular);
